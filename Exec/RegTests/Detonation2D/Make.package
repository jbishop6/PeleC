# Case sources/headers
CEXE_sources += prob.cpp
CEXE_headers += prob.H
CEXE_headers += prob_parm.H

# Ensure std::thread resolves at link time
CXXFLAGS   += -pthread
LINKFLAGS  += -pthread
LIBRARIES  += -Wl,--no-as-needed -lpthread
